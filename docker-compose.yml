services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    ports:
      - "3000:3000"
    depends_on:
      db:
        condition: service_healthy
    environment:
      - DATABASE_URL=postgresql://zradford:zradford@db:5432/docker-test-property
      - NODE_ENV=production
      - SEED_DB=true
      - PGUSER=zradford
      - PGPASSWORD=zradford
      - PGDATABASE=docker-test-property
      - PGHOST=db
      - CLIENT_ID=${CLIENT_ID}
      - CLIENT_SECRET=${CLIENT_SECRET}
    volumes:
      - ./.env:/app/.env
    networks:
      - app-network
    restart: on-failure
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/install || exit 0"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=docker-test-property
      - POSTGRES_USER=zradford
      - POSTGRES_PASSWORD=zradford
      - PGUSER=zradford
      - PGPASSWORD=zradford
      - PGDATABASE=docker-test-property
      - PGHOST=db
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zradford -d docker-test-property"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    command: ["postgres", "-c", "log_statement=all"]

networks:
  app-network:
    driver: bridge

volumes:
  postgres-data:
